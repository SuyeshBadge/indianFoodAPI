

PORT=8080
DB_PATH=mongodb://localhost:27017/indianfood # Replace with your MongoDB connection string
DB_PASSWORD=your_db_password # Replace with your MongoDB password if applicable
JWT_SECRET=your_jwt_secret # Replace with a strong, random secret for JWT
CORS_ORIGIN=http://localhost:3000 # Replace with the allowed origin for CORS, e.g., your frontend URL

